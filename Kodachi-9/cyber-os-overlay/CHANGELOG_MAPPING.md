Added node_map.yaml default mapping, mapping editor UI and loader, mapping-aware highlighting on the 3D scene, installer copy of mapping file, and a test for the tool registry discover behavior.

Files added/updated:
- frontend/kdash/config/node_map.yaml
- frontend/kdash/index.html (mapping button + modal)
- frontend/kdash/app.mapping.js (mapping loader and editor; integrated with app UI)
- scripts/install-overlay.sh (copy mapping file during install)
- tests/test_tool_registry.py
