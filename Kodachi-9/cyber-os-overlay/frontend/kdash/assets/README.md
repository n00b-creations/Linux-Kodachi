# Fetch or generate a dashboard GLB model for the overlay frontend

Run from the frontend/kdash directory (or pass a target dir):

./assets/fetch_model.sh ./assets/models

This downloads a small sample GLB (Cube) as an example. Replace with a richer glb when you have one.
